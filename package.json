{
	"name": "virtual-postcard",
	"version": "1.0.0",
	"description": "This is a ready-to-use template that allows the user to select a postcard from 4 options and fill out their name, email, and a message. The user can then preview it with the text ovelayed on the back of the card. The postcard can then be \"mailed\", which sends it off with an animation. This template can be expended with your own images and further implementation.",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"repository": {
		"type": "git",
		"url": "git+https://github.com/andrei-k/virtual-postcard.git"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"bugs": {
		"url": "https://github.com/andrei-k/virtual-postcard/issues"
	},
	"homepage": "https://github.com/andrei-k/virtual-postcard#readme",
	"dependencies": {
		"bootstrap-css-only": "^4.4.1",
		"vue": "^3.2.31"
	},
	"devDependencies": {
		"css-loader": "^6.7.1"
	}
}
